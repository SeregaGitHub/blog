DROP TABLE comment;
DROP TABLE likes;
DROP TABLE post_tag;
DROP TABLE tag;
DROP TABLE post;

CREATE TABLE IF NOT EXISTS post (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY,
  name varchar(64) NOT NULL,
  image_url varchar(512) NOT NULL,
  -- "DESCRIPTION" varchar(192) NOT NULL,
  description text[] NOT NULL,
  CONSTRAINT pk_post_id PRIMARY KEY (id),
  CONSTRAINT uk_post_name UNIQUE (name)  -- ???
);

CREATE TABLE IF NOT EXISTS tag (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY,
  name varchar(64) NOT NULL,
  --post_id INTEGER NOT NULL,
  CONSTRAINT pk_tag_id PRIMARY KEY (id),
  --CONSTRAINT uk_tag_name_post_id UNIQUE (name, post_id),
  CONSTRAINT uk_tag_name UNIQUE (name)--,
  -- CONSTRAINT fk_tag_post_id FOREIGN KEY (post_id)
  --         REFERENCES post (id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS post_tag (
	post_id INTEGER,
	tag_id INTEGER,
	CONSTRAINT pk_post_tag_id PRIMARY KEY (post_id, tag_id),
	CONSTRAINT fk_post_tag_post_id FOREIGN KEY (post_id)
          REFERENCES post (id) ON DELETE CASCADE,
	CONSTRAINT fk_post_tag_tag_id FOREIGN KEY (tag_id)
          REFERENCES tag (id) ON DELETE CASCADE
); -- create index tag_id, post_id

CREATE TABLE IF NOT EXISTS likes (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY,
  count INTEGER NOT NULL,
  post_id INTEGER NOT NULL,
  CONSTRAINT pk_likes_id PRIMARY KEY (id),
  CONSTRAINT uk_likes_post_id UNIQUE (post_id),
  CONSTRAINT fk_likes_post_id FOREIGN KEY (post_id)
          REFERENCES post (id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS comment (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY,
  post_comment varchar(2048),
  post_id INTEGER NOT NULL,
  CONSTRAINT pk_comment_id PRIMARY KEY (id),
  CONSTRAINT fk_comment_post_id FOREIGN KEY (post_id)
          REFERENCES post (id) ON DELETE CASCADE
); -- create index post_id, post_comment

---------------------------------------------------------------------------------------------------------------

INSERT INTO post (name, image_url, description)
VALUES
('post_name-1', 'url-1', ARRAY['description-11', 'description-12', 'description-13']),
('post_name-2', 'url-2', ARRAY['description-21', 'description-22', 'description-23']),
('post_name-3', 'url-3', ARRAY['description-31', 'description-32', 'description-33']),
('post_name-4', 'url-4', ARRAY['description-41', 'description-42', 'description-43'])
ON CONFLICT DO NOTHING;

INSERT INTO tag (name)
VALUES
('#meat'),
('#chees'),
('#mmm'),
('#rise')
ON CONFLICT DO NOTHING;

INSERT INTO post_tag (post_id, tag_id)
VALUES
(1, 1),
(3, 2),
(2, 3),
(1, 4)
ON CONFLICT DO NOTHING;

INSERT INTO likes (count, post_id)
VALUES
(1, 1),
(5, 2),
(4, 3)
ON CONFLICT DO NOTHING;

INSERT INTO comment (post_comment, post_id)
VALUES
('some-comment', 1),
('some-comment', 1),
('some-comment', 3)
ON CONFLICT DO NOTHING;

---------------------------------------------------------------------------------------------------------------
-- Добавление лайка. post_id = 4
INSERT INTO likes (count, post_id)
VALUES
(1, 4)
ON CONFLICT (post_id) DO UPDATE
SET count = likes.count + 1
WHERE likes.post_id = 4;
---------------------------------------------------------------------------------------------------------------

SELECT * FROM post;
SELECT * FROM tag;
SELECT * FROM post_tag;
SELECT * FROM likes;
SELECT * FROM comment;

-- Post Feed
SELECT p.id, p.name, p.image_url, SUBSTRING(p.description[1], 1, 7) AS abbreviatedDescription,
COUNT(c.post_id) AS commentCount, COALESCE (l.count, 0) AS likesCount, COALESCE (STRING_AGG(DISTINCT t.name, ' '), '') AS tags
FROM post p
LEFT JOIN comment c ON c.post_id = p.id
LEFT JOIN likes l ON l.post_id = p.id
LEFT JOIN post_tag pt ON p.id = pt.post_id
LEFT JOIN tag t ON t.id = pt.tag_id
GROUP BY p.id, p.name, c.post_id, l.count
ORDER BY p.id
OFFSET 0
LIMIT 20;


CREATE OR REPLACE FUNCTION insert_post() AS $$

	DECLARE
	post_id INTEGER;

	BEGIN
		INSERT INTO post (name, image_url, description)
		VALUES
		('post_name-5', 'url-5', ARRAY['description-51', 'description-52', 'description-53']);

		INSERT INTO tag (name)
		VALUES
		('#meat'),
		ON CONFLICT DO NOTHING;


	END;
$$ language plpgsql;

CREATE OR REPLACE PROCEDURE insert_post_tags_common(post_name varchar, image_url varchar, description text[], tags_array varchar[])
LANGUAGE SQL
AS $$

	INSERT INTO post(name, image_url, description)
	VALUES
	(post_name, image_url, description)
	--('nnn', 'uuu', ARRAY['description-51', 'description-52', 'description-53'])
	ON CONFLICT DO NOTHING
	RETURNING id --INTO post_id;

	INSERT INTO tag(name)
	VALUES (unnest(tags_array))
	ON CONFLICT DO NOTHING;

	-- INSERT INTO post_tag
	-- VALUES
	-- ()

$$;

CALL insert_post_tags('post_name-001', 'url-5', ARRAY['description-51', 'description-52', 'description-53'], ARRAY['#potatos-001', '#potatos-002', '#mmm']);


----------------------------------------------------------------------------------------------------------------------------
SELECT * FROM post;
SELECT * FROM tag;
SELECT * FROM post_tag;

CREATE OR REPLACE FUNCTION fun(post_name varchar, image_url varchar, description text[], tags_array varchar[]) RETURNS int AS $$
DECLARE
	post_id int;
BEGIN

	INSERT INTO post(name, image_url, description)
	VALUES
	(post_name, image_url, description)
	--('nnn', 'uuu', ARRAY['description-51', 'description-52', 'description-53'])
	ON CONFLICT DO NOTHING
	RETURNING id INTO post_id;

	INSERT INTO tag(name)
	VALUES (unnest(tags_array))
	ON CONFLICT DO NOTHING;

	RETURN post_id;

END;
$$ language plpgsql;

SELECT * FROM fun('post_name-001', 'url-5', ARRAY['description-51', 'description-52', 'description-53'], ARRAY['#potatos-001', '#potatos-002', '#mmm']) AS ttt;

----------------------------------------------------------------------------------------------------------------------------

CREATE PROCEDURE prprpr (IN post_name varchar, IN image_url varchar, IN description text[], IN tags_array varchar[])
LANGUAGE plpgsql
AS $$
DECLARE
  p_id INT;
BEGIN
  INSERT INTO post(name, image_url, description)
	VALUES
	(post_name, image_url, description)
	--('nnn', 'uuu', ARRAY['description-51', 'description-52', 'description-53'])
	ON CONFLICT DO NOTHING
	RETURNING id INTO p_id;

	INSERT INTO tag(name)
	VALUES (unnest(tags_array))
	ON CONFLICT DO NOTHING;


END;
$$;

----------------------------------------------------------------------------------------------------------------------------

CREATE PROCEDURE prprpr2 (IN post_name varchar, IN image_url varchar, IN description text[], IN tags_array varchar[])
LANGUAGE plpgsql
AS $$
DECLARE
  p_id INT;
  tags_id_arr int[];
BEGIN
  INSERT INTO post(name, image_url, description)
	VALUES
	(post_name, image_url, description)
	--('nnn', 'uuu', ARRAY['description-51', 'description-52', 'description-53'])
	ON CONFLICT DO NOTHING
	RETURNING id INTO p_id;

	INSERT INTO tag(name)
	VALUES (unnest(tags_array))
	ON CONFLICT DO NOTHING;

	SELECT ARRAY_AGG(t.id) FROM tag t WHERE name LIKE ANY(tags_array) INTO tags_id_arr;

	INSERT INTO post_tag
	VALUES
	-- (p_id, unnest(ARRAY[3, 4, 19, 20]));
	(p_id, unnest(tags_id_arr));
END;
$$;

CALL prprpr2 ('post_name-00224', 'url-5', ARRAY['description-51', 'description-52', 'description-53'], ARRAY['#potatos-001', '#potatos-002', '#mmm', '#rise', '#new']);