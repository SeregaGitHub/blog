DROP TABLE comment;
DROP TABLE likes;
DROP TABLE post_tag;
DROP TABLE tag;
DROP TABLE post;

CREATE TABLE IF NOT EXISTS post (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY,
  name varchar(64) NOT NULL,
  image_url varchar(512) NOT NULL,
  -- "DESCRIPTION" varchar(192) NOT NULL,
  description text[] NOT NULL,
  commentsCount INTEGER,
  CONSTRAINT pk_post_id PRIMARY KEY (id)--,
  --CONSTRAINT uk_post_name UNIQUE (name)  -- ???
);

CREATE TABLE IF NOT EXISTS tag (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY,
  name varchar(64) NOT NULL,
  --post_id INTEGER NOT NULL,
  CONSTRAINT pk_tag_id PRIMARY KEY (id),
  --CONSTRAINT uk_tag_name_post_id UNIQUE (name, post_id),
  CONSTRAINT uk_tag_name UNIQUE (name)--,
  -- CONSTRAINT fk_tag_post_id FOREIGN KEY (post_id)
  --         REFERENCES post (id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS post_tag (
	post_id INTEGER,
	tag_id INTEGER,
	CONSTRAINT pk_post_tag_id PRIMARY KEY (post_id, tag_id),
	CONSTRAINT fk_post_tag_post_id FOREIGN KEY (post_id)
          REFERENCES post (id) ON DELETE CASCADE,
	CONSTRAINT fk_post_tag_tag_id FOREIGN KEY (tag_id)
          REFERENCES tag (id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS likes (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY,
  count INTEGER NOT NULL,
  post_id INTEGER NOT NULL,
  CONSTRAINT pk_likes_id PRIMARY KEY (id),
  CONSTRAINT uk_likes_post_id UNIQUE (post_id),
  CONSTRAINT fk_likes_post_id FOREIGN KEY (post_id)
          REFERENCES post (id) ON DELETE CASCADE
); -- create index post_id, count ??? - UNIQUE (post_id) exists

CREATE TABLE IF NOT EXISTS comment (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY,
  post_comment varchar(2048),
  post_id INTEGER NOT NULL,
  CONSTRAINT pk_comment_id PRIMARY KEY (id),
  CONSTRAINT fk_comment_post_id FOREIGN KEY (post_id)
          REFERENCES post (id) ON DELETE CASCADE
); -- create index post_id, post_comment ???

------------------------------------------------------------------------------------------------------------------------
-- index for the tag-help quick search in posts
CREATE INDEX idx_post_tag_tag_id_post_id ON post_tag(tag_id, post_id);

-- index for the quick search comments to one post
CREATE INDEX idx_comment_post_id_post_comment ON comment(post_id, post_comment);
------------------------------------------------------------------------------------------------------------------------

INSERT INTO post (name, image_url, description, commentsCount)
VALUES
('post_name-1', 'url-1', ARRAY['description-11', 'description-12', 'description-13'], 2),
('post_name-2', 'url-2', ARRAY['description-21', 'description-22', 'description-23'], 0),
('post_name-3', 'url-3', ARRAY['description-31', 'description-32', 'description-33'], 1),
('post_name-4', 'url-4', ARRAY['description-41', 'description-42', 'description-43'], 0)
ON CONFLICT DO NOTHING;

INSERT INTO tag (name)
VALUES
('#meat'),
('#chees'),
('#mmm'),
('#rise')
ON CONFLICT DO NOTHING;

INSERT INTO post_tag (post_id, tag_id)
VALUES
(1, 1),
(3, 2),
(2, 3),
(1, 4),
(4, 3)
ON CONFLICT DO NOTHING;

INSERT INTO likes (count, post_id)
VALUES
(1, 1),
(5, 2),
(4, 3)
ON CONFLICT DO NOTHING;

INSERT INTO comment (post_comment, post_id)
VALUES
('some-comment', 1),
('some-comment', 1),
('some-comment', 3)
ON CONFLICT DO NOTHING;

------------------------------------------------------------------------------------------------------------------------

SELECT * FROM post;
SELECT * FROM tag;
SELECT * FROM post_tag;
SELECT * FROM likes;
SELECT * FROM comment;

------------------------------------------------------------------------------------------------------------------------

-- Post Feed
SELECT p.id, p.name, p.image_url, SUBSTRING(p.description[1], 1, 7) AS abbreviatedDescription,
p.commentsCount, COALESCE (l.count, 0) AS likesCount, COALESCE (STRING_AGG(t.name, ' '), '') AS tags
FROM post p
LEFT JOIN likes l ON l.post_id = p.id
LEFT JOIN post_tag pt ON p.id = pt.post_id
LEFT JOIN tag t ON t.id = pt.tag_id
GROUP BY p.id, l.count
ORDER BY p.id DESC
OFFSET 0
LIMIT 20;

------------------------------------------------------------------------------------------------------------------------

-- filtering by tag
SELECT p.id, p.name, p.image_url, SUBSTRING(p.description[1], 1, 7) AS abbreviatedDescription,
p.commentsCount, COALESCE (l.count, 0) AS likesCount, COALESCE (STRING_AGG(t.name, ' '), '') AS tags
FROM tag t
JOIN post_tag pt ON t.id = pt.tag_id
JOIN post p ON p.id = pt.post_id
LEFT JOIN likes l ON l.post_id = p.id
WHERE p.id IN (SELECT pt.post_id
			   FROM post_tag pt
			   JOIN tag t ON pt.tag_id = t.id
			   WHERE t.name = '#mmm')
GROUP BY p.id, p.name, l.count
ORDER BY p.id DESC
OFFSET 0
LIMIT 20;

-- old version
SELECT p.id, p.name, p.image_url, SUBSTRING(p.description[1], 1, 7) AS abbreviatedDescription,
p.commentsCount, COALESCE (l.count, 0) AS likesCount, COALESCE (STRING_AGG(t.name, ' '), '') AS tags
FROM post p
LEFT JOIN likes l ON l.post_id = p.id
LEFT JOIN post_tag pt ON p.id = pt.post_id
LEFT JOIN tag t ON t.id = pt.tag_id
WHERE t.name = '#mmm'
GROUP BY p.id, l.count
--HAVING t.name = '#mmm'
ORDER BY p.id DESC;

------------------------------------------------------------------------------------------------------------------------

-- create new post
CREATE PROCEDURE create_new_post(IN post_name varchar, IN image_url varchar, IN description text[], IN tags_array varchar[])
LANGUAGE plpgsql
AS $$
DECLARE
  p_id INT;
  tags_id_arr int[];
BEGIN
  INSERT INTO post(name, image_url, description, commentsCount)
	VALUES
	(post_name, image_url, description, 0)
	ON CONFLICT DO NOTHING
	RETURNING id INTO p_id;

	INSERT INTO tag(name)
	VALUES (unnest(tags_array))
	ON CONFLICT DO NOTHING;

	SELECT ARRAY_AGG(t.id) FROM tag t WHERE name LIKE ANY(tags_array) INTO tags_id_arr;

	INSERT INTO post_tag
	VALUES
	(p_id, unnest(tags_id_arr));
END;
$$;

CALL create_new_post('post_name-002', 'url-002', ARRAY['description-51', 'description-52', 'description-53'],
					 ARRAY['#potatos-001', '#potatos-002', '#mmm', '#rise', '#new']);

------------------------------------------------------------------------------------------------------------------------

-- select one post
SELECT p.id, p.name, p.image_url, p.description,
		COALESCE (STRING_AGG(DISTINCT t.name, ' '), '') AS tags,
		COALESCE (l.count, 0) AS likesCount,
		CASE
    		WHEN length((ARRAY_AGG(c.post_comment))[1]) > 0 THEN (ARRAY_AGG(DISTINCT c.post_comment))
    		ELSE ARRAY['']
		END AS comments
FROM post p
LEFT JOIN comment c ON c.post_id = p.id
LEFT JOIN likes l ON l.post_id = p.id
LEFT JOIN post_tag pt ON p.id = pt.post_id
LEFT JOIN tag t ON t.id = pt.tag_id
WHERE p.id = 7
GROUP BY p.id, p.name, l.count;

------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------
-- Добавление лайка. (post_id = 5)
INSERT INTO likes (count, post_id)
VALUES
(1, 5)
ON CONFLICT (post_id) DO UPDATE
SET count = likes.count + 1
WHERE likes.post_id = 5;

------------------------------------------------------------------------------------------------------------------------
-- кнопка добавления комментария. (post_id = 7)
BEGIN;

	INSERT INTO comment(post_comment, post_id)
	VALUES
	('new-comment 3 for post = 7', 7);

	UPDATE post
	SET commentsCount = commentsCount + 1
	WHERE id = 7;

COMMIT;
------------------------------------------------------------------------------------------------------------------------
-- возможность редактирования комментария
UPDATE comment
SET post_comment = 'updated-comment 3 for post = 7'
WHERE id = 6;

-- возможность удаления комментария
BEGIN;

	DELETE FROM comment
	WHERE id = 5;

	UPDATE post
	SET commentsCount = commentsCount - 1
	WHERE id = 7;

COMMIT;
------------------------------------------------------------------------------------------------------------------------
-- кнопка редактирования поста






-- кнопка удаления поста
DELETE FROM post
WHERE id = 7;




