DROP TABLE comment;
DROP TABLE likes;
DROP TABLE post_tag;
DROP TABLE tag;
DROP TABLE post;

CREATE TABLE IF NOT EXISTS post (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY,
  name varchar(64) NOT NULL,
  image_url varchar(512) NOT NULL,
  description text[] NOT NULL,
  commentsCount INTEGER DEFAULT 0,
  CONSTRAINT pk_post_id PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS tag (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY,
  name varchar(64) NOT NULL,
  CONSTRAINT pk_tag_id PRIMARY KEY (id),
  CONSTRAINT uk_tag_name UNIQUE (name)
);

CREATE TABLE IF NOT EXISTS post_tag (
	post_id INTEGER,
	tag_id INTEGER,
	CONSTRAINT pk_post_tag_id PRIMARY KEY (post_id, tag_id),
	CONSTRAINT fk_post_tag_post_id FOREIGN KEY (post_id)
          REFERENCES post (id) ON DELETE CASCADE,
	CONSTRAINT fk_post_tag_tag_id FOREIGN KEY (tag_id)
          REFERENCES tag (id) --ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS likes (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY,
  count INTEGER NOT NULL,
  post_id INTEGER NOT NULL,
  CONSTRAINT pk_likes_id PRIMARY KEY (id),
  CONSTRAINT uk_likes_post_id UNIQUE (post_id),
  CONSTRAINT fk_likes_post_id FOREIGN KEY (post_id)
          REFERENCES post (id) ON DELETE CASCADE
); -- create index post_id, count ??? - UNIQUE (post_id) exists

CREATE TABLE IF NOT EXISTS comment (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY,
  post_comment varchar(2048),
  post_id INTEGER NOT NULL,
  CONSTRAINT pk_comment_id PRIMARY KEY (id),
  CONSTRAINT fk_comment_post_id FOREIGN KEY (post_id)
          REFERENCES post (id) ON DELETE CASCADE
);

------------------------------------------------------------------------------------------------------------------------
-- index for the tag-help quick search in posts
CREATE INDEX idx_post_tag_tag_id_post_id ON post_tag(tag_id, post_id);

-- index for the quick search comments to one post
CREATE INDEX idx_comment_post_id_post_comment ON comment(post_id, post_comment);
------------------------------------------------------------------------------------------------------------------------

INSERT INTO post (name, image_url, description, commentsCount)
VALUES
('post_name-1', 'url-1', ARRAY['description-11', 'description-12', 'description-13'], 2),
('post_name-2', 'url-2', ARRAY['description-21', 'description-22', 'description-23'], 0),
('post_name-3', 'url-3', ARRAY['description-31', 'description-32', 'description-33'], 1),
('post_name-4', 'url-4', ARRAY['description-41', 'description-42', 'description-43'], 0)
ON CONFLICT DO NOTHING;

INSERT INTO tag (name)
VALUES
('#meat'),
('#chees'),
('#mmm'),
('#rise')
ON CONFLICT DO NOTHING;

INSERT INTO post_tag (post_id, tag_id)
VALUES
(1, 1),
(3, 2),
(2, 3),
(1, 4),
(4, 3),
(1, 3)
ON CONFLICT DO NOTHING;

INSERT INTO likes (count, post_id)
VALUES
(1, 1),
(5, 2),
(4, 3)
ON CONFLICT DO NOTHING;

INSERT INTO comment (post_comment, post_id)
VALUES
('some-comment-11', 1),
('some-comment-12', 1),
('some-comment-31', 3)
ON CONFLICT DO NOTHING;

------------------------------------------------------------------------------------------------------------------------

SELECT * FROM post;
SELECT * FROM tag;
SELECT * FROM post_tag;
SELECT * FROM likes;
SELECT * FROM comment;

------------------------------------------------------------------------------------------------------------------------

-- Post Feed
SELECT p.id, p.name, p.image_url, SUBSTRING(p.description[1], 1, 7) AS abbreviatedDescription,
p.commentsCount, COALESCE (l.count, 0) AS likesCount, COALESCE (STRING_AGG(t.name, ' '), '') AS tags
FROM post p
LEFT JOIN likes l ON l.post_id = p.id
LEFT JOIN post_tag pt ON p.id = pt.post_id
LEFT JOIN tag t ON t.id = pt.tag_id
GROUP BY p.id, l.count
ORDER BY p.id DESC
OFFSET 0
LIMIT 20;

------------------------------------------------------------------------------------------------------------------------

-- filtering by tag
SELECT p.id, p.name, p.image_url, SUBSTRING(p.description[1], 1, 7) AS abbreviatedDescription,
p.commentsCount, COALESCE (l.count, 0) AS likesCount, COALESCE (STRING_AGG(t.name, ' '), '') AS tags
FROM tag t
JOIN post_tag pt ON t.id = pt.tag_id
JOIN post p ON p.id = pt.post_id
LEFT JOIN likes l ON l.post_id = p.id
WHERE p.id IN (SELECT pt.post_id
			   FROM post_tag pt
			   JOIN tag t ON pt.tag_id = t.id
			   WHERE t.name = '#mmm')
GROUP BY p.id, l.count
ORDER BY p.id DESC
OFFSET 0
LIMIT 20;

-- old version
SELECT p.id, p.name, p.image_url, SUBSTRING(p.description[1], 1, 7) AS abbreviatedDescription,
p.commentsCount, COALESCE (l.count, 0) AS likesCount, COALESCE (STRING_AGG(t.name, ' '), '') AS tags
FROM post p
LEFT JOIN likes l ON l.post_id = p.id
LEFT JOIN post_tag pt ON p.id = pt.post_id
LEFT JOIN tag t ON t.id = pt.tag_id
WHERE t.name = '#mmm'
GROUP BY p.id, l.count
--HAVING t.name = '#mmm'
ORDER BY p.id DESC;

------------------------------------------------------------------------------------------------------------------------

-- create new post
CREATE OR REPLACE PROCEDURE create_new_post(IN post_name varchar, IN image_url varchar, IN description text[], IN tags_array varchar[])
LANGUAGE plpgsql
AS $$
DECLARE
  p_id INT;
  tags_id_arr int[];
BEGIN
  INSERT INTO post(name, image_url, description, commentsCount)
	VALUES
	(post_name, image_url, description, 0)
	ON CONFLICT DO NOTHING
	RETURNING id INTO p_id;

	INSERT INTO tag(name)
	VALUES (unnest(tags_array))
	ON CONFLICT DO NOTHING;

	SELECT ARRAY_AGG(t.id) FROM tag t WHERE name LIKE ANY(tags_array) INTO tags_id_arr;

	INSERT INTO post_tag
	VALUES
	(p_id, unnest(tags_id_arr));
END;
$$;

BEGIN TRANSACTION ISOLATION LEVEL REPEATABLE READ;
CALL create_new_post('post_name-002', 'url-002', ARRAY['description-51', 'description-52', 'description-53'],
					 ARRAY['#potatos-001', '#potatos-002', '#mmm', '#rise', '#new']);
COMMIT;

------------------------------------------------------------------------------------------------------------------------

-- select one post
SELECT p.id, p.name, p.image_url, p.description,
		COALESCE (STRING_AGG(DISTINCT t.name, ' '), '') AS tags,
		COALESCE (l.count, 0) AS likesCount,
		CASE
    		WHEN length((ARRAY_AGG(c.post_comment))[1]) > 0 THEN (ARRAY_AGG(DISTINCT c.post_comment))
    		ELSE ARRAY['']
		END AS comments
FROM post p
LEFT JOIN comment c ON c.post_id = p.id
LEFT JOIN likes l ON l.post_id = p.id
LEFT JOIN post_tag pt ON p.id = pt.post_id
LEFT JOIN tag t ON t.id = pt.tag_id
WHERE p.id = 5
GROUP BY p.id, l.count;

------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------
-- Добавление лайка. (post_id = 5)
INSERT INTO likes (count, post_id)
VALUES
(1, 5)
ON CONFLICT (post_id) DO UPDATE
SET count = likes.count + 1
WHERE likes.post_id = 5;

------------------------------------------------------------------------------------------------------------------------
-- кнопка добавления комментария. (post_id = 5)
BEGIN;

	INSERT INTO comment(post_comment, post_id)
	VALUES
	('new-comment 1 for post = 5', 5);

	UPDATE post
	SET commentsCount = commentsCount + 1
	WHERE id = 5;

COMMIT;
------------------------------------------------------------------------------------------------------------------------
-- возможность редактирования комментария
UPDATE comment
SET post_comment = 'updated-comment 1 for post = 5'
WHERE id = 4;

-- возможность удаления комментария
BEGIN;

	DELETE FROM comment
	WHERE id = 5;

	UPDATE post
	SET commentsCount = commentsCount - 1
	WHERE id = 5;

COMMIT;
------------------------------------------------------------------------------------------------------------------------
-- кнопка редактирования поста
-- update post (post_id = 5)
CREATE OR REPLACE PROCEDURE update_post(IN update_post_id INTEGER, IN post_name varchar, IN post_image_url varchar, IN post_description text[], IN tags_array varchar[])
LANGUAGE plpgsql
AS $$
DECLARE
  tags_id_arr int[];
BEGIN
	UPDATE post
	SET
	name = post_name,
	image_url = post_image_url,
	description = post_description
	WHERE id = update_post_id;

	INSERT INTO tag(name)
	VALUES (unnest(tags_array))
	ON CONFLICT DO NOTHING;

	SELECT ARRAY_AGG(t.id) FROM tag t WHERE name LIKE ANY(tags_array) INTO tags_id_arr;

	DELETE FROM post_tag
	WHERE post_id = update_post_id
	AND tag_id <> ANY(tags_id_arr);

	INSERT INTO post_tag
	VALUES
	(update_post_id, unnest(tags_id_arr))
	ON CONFLICT DO NOTHING;
END;
$$;

BEGIN TRANSACTION ISOLATION LEVEL REPEATABLE READ;
CALL update_post(5, 'post_name-002', 'url-222', ARRAY['updated-description', 'description-53'],
					 ARRAY['#after-update', '#mmm', '#rise', '#new']);
COMMIT;

-- кнопка удаления поста
DELETE FROM post
WHERE id = 5;

------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------
-- кнопка редактирования поста
-- update post (post_id = 5)
-- Experiment !!!
CREATE PROCEDURE update_post(IN update_post_id INTEGER, IN post_name varchar, IN post_image_url varchar, IN post_description text[], IN tags_array varchar[])
LANGUAGE plpgsql
AS $$
DECLARE
  new_tags_id_arr int[];
  old_tags_id_arr int[];
  i int;
BEGIN
	UPDATE post
	SET
	name = post_name,
	image_url = post_image_url,
	description = post_description
	WHERE id = update_post_id;

	SELECT ARRAY_AGG(pt.tag_id) FROM post_tag pt WHERE pt.post_id = update_post_id INTO old_tags_id_arr;

	INSERT INTO tag(name)
	VALUES (unnest(tags_array))
	ON CONFLICT DO NOTHING;

	SELECT ARRAY_AGG(t.id) FROM tag t WHERE name LIKE ANY(tags_array) INTO new_tags_id_arr;

	DELETE FROM post_tag
	WHERE post_id = update_post_id
	AND tag_id <> ANY(new_tags_id_arr);

	INSERT INTO post_tag
	VALUES
	(update_post_id, unnest(new_tags_id_arr))
	ON CONFLICT DO NOTHING;

	DELETE FROM tag
	WHERE id = ANY(old_tags_id_arr);

	FOREACH i IN ARRAY old_tags_id_arr
    	LOOP

    		DELETE FROM tag
			WHERE id = i;

    	END LOOP;

	--EXEC SQL WHENEVER SQLERROR CONTINUE;

		EXCEPTION WHEN SQLSTATE '23503' THEN NULL;

	-- EXCEPTION
 --    WHEN SQLSTATE '23503' THEN
 --    RAISE NOTICE 'Illegal operation: %', SQLERRM;

	-- ON CONFLICT DO NOTHING;

	-- DELETE FROM tag
	-- WHERE id = ANY(ARRAY[20, 38])
	-- ON CONFLICT DO NOTHING;

	-- 23503
END;
$$;

------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------

CREATE OR REPLACE FUNCTION toggle_subscription(
        pid NUMERIC,
        tid NUMERIC
    )
    RETURNS NUMERIC AS $$
    DECLARE
        row_exists NUMERIC;
    BEGIN
        SELECT 1 INTO row_exists FROM thread_subscription WHERE profile_id = pid and thread_id = tid;
        IF (row_exists > 0) THEN
            DELETE FROM thread_subscription WHERE profile_id = pid and thread_id = tid;
            RETURN 0;
        ELSE
            INSERT INTO thread_subscription(profile_id, thread_id) VALUES(pid, tid);
            RETURN 1;
        END IF;
    END;
    $$
    LANGUAGE plpgsql;



CREATE OR REPLACE FUNCTION toggle_subscription(
    pid NUMERIC,
    tid NUMERIC
)
RETURNS NUMERIC AS $$

BEGIN

    perform FROM thread_subscription WHERE profile_id = pid and thread_id = tid;
    IF NOT FOUND THEN
        INSERT INTO thread_subscription(profile_id, thread_id) VALUES(pid, tid);
        RETURN 1;

    ELSE
         DELETE FROM thread_subscription WHERE profile_id = pid and thread_id = tid;
        RETURN 0;
    END IF;
END;
$$
LANGUAGE plpgsql;